<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/add_record"
        android:title="@string/add_record"
        android:icon="@drawable/ic_add_record"
        app:showAsAction="always"/>

    <item android:id="@+id/delete_records"
        android:title="@string/delete_record"
        android:icon="@drawable/ic_delete_record"
        app:showAsAction="always"/>

</menu>