<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/delete_record"
        android:icon="@drawable/ic_delete_record"
        android:title="@string/delete_record"
        app:showAsAction="always" />

    <item android:id="@+id/save_routine"
        android:title="@string/save_routine"
        app:showAsAction="never" />

    <item android:id="@+id/load_routine"
        android:title="@string/load_routine"
        app:showAsAction="never"/>

</menu>