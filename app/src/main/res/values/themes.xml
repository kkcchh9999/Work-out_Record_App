<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Work_out_Record" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/green</item>
        <item name="colorOnPrimary">@color/gray</item>
        <item name="android:textColorPrimary">@color/gray</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/green1</item>
        <!-- Customize your theme here. -->

        <item name="iconTint">@color/white</item>
        <item name="android:navigationBarColor">@color/green1</item>
        <item name="colorControlActivated">@color/green2</item>
        <item name="colorAccent">@color/green2</item>

    </style>
    <style name="PopupMenuStyle" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/green</item>
        <item name="android:textColor">@color/gray </item>
    </style>

</resources>